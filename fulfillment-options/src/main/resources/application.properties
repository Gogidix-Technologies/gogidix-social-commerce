# fulfillment-options configuration
spring.application.name=fulfillment-options
server.port=8080

# Database configuration (update as needed)
# spring.datasource.url=jdbc:postgresql://localhost:5432/fulfillment-options_db
# spring.datasource.username=postgres
# spring.datasource.password=password

# Logging
logging.level.root=INFO
logging.level.com.exalt=DEBUG

# Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
